# Off-Policy Corrected Fine-Tuning of Recommender Policies

## Working Title

Off-Policy Corrected Fine-Tuning of Recommender Policies from Logged Implicit Feedback

## One-Sentence Thesis

This thesis studies whether using logged behavior propensities during policy fine-tuning improves recommender policy learning and model selection compared with a no-propensity baseline that optimizes pure naive reward without propensity correction.

## Abstract Skeleton

Recommender systems are commonly trained and improved using historical interaction logs. These logs are not random samples: they are generated by an existing behavior policy, which determines which items users were exposed to and therefore which rewards can be observed. This creates a central difficulty for fine-tuning recommender policies from logged data: directly optimizing on observed rewards may reinforce logging-policy bias rather than improve the target policy.

This thesis investigates off-policy corrected fine-tuning for recommender systems under contextual bandit feedback. Real implicit-feedback datasets are first converted into user-item interaction matrices and used to train Bayesian Personalized Ranking (BPR) matrix-factorization embeddings. These embeddings define a semi-synthetic bandit environment in which latent user and item factors determine ground-truth reward probabilities, while noisy versions of the embeddings represent the information available to the learner. A collaborative-filtering policy is initialized from these noisy embeddings and fine-tuned using logged rewards.

The main experimental comparison isolates the effect of propensity correction. The proposed off-policy correction (OPC) setting uses logged propensities in IW/DR-style policy-gradient objectives (default unified `kl_crm` = SNDR + KL + CRM variance). The no-propensity baseline uses the same model family, logged data, validation splits, shared reward model, and hyperparameter search, but trains with pure naive `mean(r * pi)` (`propensity_mode=uniform`, no DM/SNDR/IW/KL/CRM). Experiments sweep datasets, noise levels (including extreme/brutal), noise axes, CTR regimes, train/val sizes, seeds, logging–uniform mix, policy temperature, policy losses, Optuna selection metrics, and reward-model source (`--reward-model`).

Results should report whether propensity-aware training improves true policy value, validation-based policy selection, and robustness relative to the no-propensity baseline, and under which logging-bias/noise regimes the correction is most beneficial.

## Paper Structure

### 1. Introduction

Goal: explain the motivation and the central research question.

Main points:

- Recommender systems learn from logged user interactions.
- Logged interactions are collected by a behavior policy, so observed rewards are exposure-biased.
- Fine-tuning a recommender on logged rewards is therefore an off-policy learning problem.
- Off-policy correction can reduce bias, but may increase variance through large importance weights.
- The thesis asks when propensity-aware fine-tuning improves recommender policy learning over a no-propensity baseline.

Suggested contribution bullets:

- A controlled semi-synthetic evaluation framework built from real recommender datasets and BPR embeddings.
- A policy fine-tuning pipeline with losses `kl_crm` (default), `kl`, `ipw`, `sndr`, `crm`, and `naive`.
- A matched ablation comparing propensity-aware OPC to a pure-naive no-propensity baseline under identical splits and search budgets.
- A systematic study across dataset, noise, CTR, train-size, validation-size, and seed conditions.

### 2. Background

Goal: define the concepts needed to read the method.

Subsections:

- Implicit-feedback recommendation and BPR matrix factorization.
- Contextual bandits for recommendation.
- Logged bandit feedback, behavior policy, target/evaluation policy, and propensity scores.
- Off-policy evaluation and learning.
- Inverse propensity weighting (IPW).
- Direct method (DM), doubly robust (DR), and self-normalized doubly robust (SNDR) estimators.
- KL regularization toward the logging policy.

Key notation:

- `x`: user/context.
- `a`: item/action.
- `r`: observed binary reward.
- `pi_b(a | x)`: behavior/logging policy.
- `pi_theta(a | x)`: learned target policy.
- `p_i = pi_b(a_i | x_i)`: logged propensity.
- `w_i = pi_theta(a_i | x_i) / pi_b(a_i | x_i)`: importance weight.
- `q_hat(x, a)`: learned reward model.
- `V(pi)`: true policy value.

### 3. Problem Setup

Goal: formally describe the learning problem.

The learner receives logged tuples:

```text
D = {(x_i, a_i, r_i, p_i)}_{i=1}^n
```

where each action `a_i` was sampled by a behavior policy `pi_b`, and `p_i` is the probability assigned to that logged action. The objective is to learn a target policy `pi_theta` that maximizes expected reward:

```text
V(pi_theta) = E_x E_{a ~ pi_theta(. | x)} [r(x, a)]
```

The main empirical question is whether using `p_i` during training improves the learned policy compared with an otherwise identical method that ignores `p_i`.

### 4. Data and Environment Construction

Goal: explain how real datasets become a controlled bandit simulator.

Code references:

- Dataset loading: `BPR/dataload.py`
- BPR training: `BPR/bpr.py`
- Artifact generation: `BPR/generate_artifacts.py`
- Synthetic environment: `utils/simulation_utils.py`

Pipeline:

1. Load implicit-feedback recommender datasets.
2. Build sparse user-item interaction matrices.
3. Train BPR matrix factorization to obtain user and item embeddings.
4. Treat the clean embeddings as latent ground-truth reward factors.
5. Generate noisy learner-facing embeddings by mixing clean embeddings with structured noise.
6. Define binary reward probabilities from user-item dot products and a CTR parameter.
7. Generate logged bandit data by sampling actions from a softmax logging policy over noisy embeddings.

Datasets supported by the code:

- MovieLens 1M (`ml`)
- Myket (`myket`)
- Anime (`anime`)
- LastFM (`lastfm`)
- Million Song Dataset taste profile style data (`msd`)

Noise modes and axes:

- `kmeans_templates`
- `random_centroids`
- context noise
- action noise
- metadata noise
- combined noise
- low, medium, high noise levels

### 5. Policy Model

Goal: describe the policy family.

The target policy is a collaborative-filtering softmax policy over user and item embeddings:

```text
pi_theta(a | x) = softmax(u_theta(x)^T v_theta(a) / tau)
```

The code uses two main variants:

- `CFModel`: trainable user/item embeddings with optional transforms.
- `LinearCFModel`: linear delta transforms over frozen initial embeddings.

The regression trainer primarily uses `CFModel` with nonlinear residual transforms:

- `SingleMLPTransform` for user embeddings.
- `SingleMLPTransform` for item embeddings.

Relevant code:

- `models/models.py`
- `training/trainer_trials.py`

### 6. Reward Model

Goal: explain `q_hat(x, a)`.

The reward model estimates expected reward for user-action pairs. Full-study default fits a logistic `RegressionModel` on concatenated noisy user/action embeddings. Ablation flag `--reward-model`:

- `regression` (default): learned LR on `our_x`/`our_a`
- `logging_score`: CTR link on noisy embeddings (no fit)
- `oracle`: CTR link on clean env embeddings (sim-only upper bound)

Also available in code: MLP and neighborhood reward models (not the full-study default).

Relevant code:

- `fit_shared_regression_bundle` / `AnalyticRewardModel` in `training/trainer_trials.py`
- `RegressionModel` in `models/models.py`
- `MLPRewardModel` in `models/models.py`
- `NeighborhoodModel` in `models/models.py`

The reward model is used for direct-method and doubly robust terms during validation and policy scoring. See `docs/training_losses.md` §1.1.

### 7. Training Objectives

Goal: explain the losses. See `docs/training_losses.md` for full formulas.

Relevant code:

- `models/custom_losses.py`
- `training/trainer_trials.py` (`VALID_POLICY_LOSSES`)

Supported names: `kl_crm` (default), `kl`, `ipw`, `sndr`, `crm`, `naive`.

#### Default OPC: `kl_crm`

```text
Loss = -SNDR surrogate + kl_gamma * KL_MC + crm_lambda * sqrt(Var(u) / n)
```

with `u_i = -r_i * min(w_i, crm_M)`.

#### IPW

```text
w_i = pi_theta(a_i | x_i) / p_i
```

Log-trick: `-mean(stopgrad(w) * r * log pi)`. Direct: `-mean(w * r)`.

#### SNDR

```text
DM_i = sum_a q_hat(x_i, a) pi_theta(a | x_i)
Correction_i = w_i (r_i - q_hat(x_i, a_i)) / mean(w)
r_hat_i = DM_i + Correction_i
```

#### KL-Regularized Objective (`kl`)

```text
Loss = -SNDR surrogate + gamma * KL(pi_b || pi_theta)
```

KL is estimated on logged actions:

```text
mean(log pi_b(a_i | x_i) - log pi_theta(a_i | x_i))
```

#### Naive (`naive`; no-propensity default)

```text
Loss = -mean(r_i * pi_theta(a_i | x_i))
```

No IW, DM, SNDR, KL, or CRM.

### 8. Main Ablation: OPC vs No-Propensity

Goal: make the central comparison extremely clear.

```text
opc:           propensity_mode="logged",  default policy_loss=kl_crm, use_log_trick fixed True
no_propensity: propensity_mode="uniform", policy_loss=naive,         use_log_trick fixed False
```

OPC importance weights:

```text
w_i = pi_theta(a_i | x_i) / pi_b(a_i | x_i)
```

No-propensity does **not** run SNDR/IPW with `w_i = 1`. It uses pure naive
`mean(r * pi)` and ignores `pscore` / reward-model scores in the policy loss.

Everything else is held fixed:

- Same logged train/validation splits.
- Same policy model.
- Same shared reward model.
- Same Optuna search budget.
- Same train sizes.
- Same validation sizes.
- Same final evaluation code.

Relevant code:

- `regression_trainer_trial` in `training/trainer_trials.py`
- `no_propensity_trainer_trial` in `training/trainer_trials.py`
- `_run_condition` / `_no_prop_policy_loss_types` in `training/run_full_study.py`

### 9. Hyperparameter Selection

Goal: explain how policies are selected.

Optuna tunes:

- learning rate
- number of epochs
- batch size
- learning-rate decay
- `kl_gamma` when the loss needs KL (`kl`, `kl_crm`)
- `crm_M`, `crm_lambda` when the loss needs CRM (`crm`, `kl_crm`)
- policy loss type, if multiple `--policy-losses` are enabled

Log trick is fixed in the full study (OPC True, no-prop False), not searched.

`--optuna-selection` chooses the scalar to maximize:

```text
ci_low (default) = mean(row_value) - t_crit * SE
r_hat            = mean(row_value)
actual_reward    = true simulator reward (oracle / debug)
```

OPC `row_value` is DR (`DM + w (r - q)`). No-prop `row_value` is `r * pi`.
Recent ablations often use `sndr`/`ipw` with hurt logging and `r_hat`.

### 10. Evaluation Metrics

Goal: identify the result columns to report.

Important metrics from the code:

- `policy_rewards`: true policy value under the simulator.
- `actual_reward`: true reward for Optuna trial policies.
- `initial_reward`: true value of the initial logging/noisy policy.
- `selection_val_score`: validation score used for model selection.
- `reward_pct_vs_initial`: improvement relative to the initial policy.
- `opc - no_propensity` paired deltas.
- `ess`: effective sample size of importance weights.
- `max_wi`, `min_wi`, `gini`: importance-weight diagnostics.
- DR, IPW, SNDR, and DM estimates in non-slim evaluation.

Relevant analysis code:

- `training/analyze_full_study.py`
- `training/plot_actual_reward_opc_vs_noprop.py`
- `training/plot_hyperparam_reward_sensitivity.py`
- `training/plot_ablation_ctr_axis.py`

### 11. Experimental Sweeps

Goal: list the controlled variables.

From `training/run_full_study.py` and `training/run_full_study_parallel.py`:

- datasets
- noise modes
- noise axes
- noise levels: `low`, `medium`, `high`, `extreme`, `brutal`
- CTR levels
- train sizes
- validation sizes
- seeds
- policy losses (`kl_crm`, `kl`, `ipw`, `sndr`, `crm`, `naive`)
- Optuna selection (`ci_low`, `r_hat`, `actual_reward`)
- logging–uniform mix `α` (`--logging-uniform-mix`)
- reward model (`regression`, `logging_score`, `oracle`)
- policy temperature (`--policy-temperature`)
- shared regression training size

Default full-study style settings include:

- train sizes: `5000`, `25000`, `50000`, `100000`
- default CTR: `0.05`
- default policy loss: `kl_crm`
- OPC log trick fixed True; no-propensity log trick fixed False
- default Optuna selection: `ci_low`
- default policy temperature: `1.0`
- default validation size: either fixed or derived from `val_frac`, `val_min`, and `val_max`

### 12. Results Plan

Goal: decide what tables and figures the thesis needs.

Recommended tables:

- Dataset statistics after preprocessing: users, items, interactions, embedding dimension.
- Main OPC vs no-propensity result table by dataset/noise/train size.
- Mean percentage improvement over initial policy.
- Paired OPC minus no-propensity deltas with confidence intervals.
- Hyperparameter sensitivity table: KL gamma, loss type, log trick.

Recommended figures:

- Selected true policy reward versus train size.
- OPC minus no-propensity delta versus train size.
- Percentage improvement over initial policy.
- Scatter of validation score versus actual reward.
- Effective sample size versus actual reward.
- Validation-size sensitivity for selected policy reward.
- Noise-axis and CTR ablations.

### 13. Discussion

Goal: interpret the results after they are filled in.

Questions to answer:

- When does propensity correction help most?
- Does OPC help more under stronger logging-policy bias?
- Does higher noise make correction more or less useful?
- Does correction improve model selection or only final training?
- Are gains limited by reward-model accuracy?
- When do importance weights create too much variance?
- Does KL regularization stabilize training?
- Does the no-propensity baseline ever outperform OPC, and why?

### 14. Limitations

Likely limitations:

- The reward environment is semi-synthetic, even though it is grounded in real recommender data.
- BPR embeddings define the latent reward model, so conclusions depend on that factorization.
- Logged propensities are known exactly in the simulator; real systems may only estimate them.
- Reward-model misspecification may affect DR-based validation.
- Large action catalogs require chunked computation and create high computational cost.
- The no-propensity baseline is strong but still shares the same simulated setting.

### 15. Conclusion

Goal: restate the thesis after results.

Template:

This thesis presented a controlled study of off-policy corrected fine-tuning for recommender systems trained from logged bandit feedback. By grounding the simulator in BPR embeddings from real implicit-feedback datasets and comparing propensity-aware training to a matched no-propensity ablation, the study isolates the practical value of behavior-policy correction. The results show that [insert finding], suggesting that propensity correction is [beneficial/conditional/unstable] for recommender fine-tuning under [insert regimes]. These findings highlight the importance of modeling exposure bias when using logged interactions to improve recommendation policies.

